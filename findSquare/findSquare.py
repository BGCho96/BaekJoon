N, M = map(int,input().split())

box = []
for n in range(N):
    box.append(list(map(int,input())))
numbers=[i for i in range(0,N*M)]
print(box)
# while numbers:
#     startPoint=numbers.pop()
#     for endpoint in numbers:
        
        







